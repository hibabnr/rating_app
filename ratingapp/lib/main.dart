import 'package:flutter/material.dart';
import 'app.dart';

void main() => runApp(const MyApp());


/*import 'package:flutter/material.dart';
import 'custom_drawer.dart'; // Import the CustomDrawer

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Navbar Example',
      theme: ThemeData(
        primaryColor: Color(0xFF2456A0),
      ),
      home: MyHomePage(),
    );
  }
}

class MyHomePage extends StatefulWidget {
  @override
  _MyHomePageState createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFF2456A0),
        title: Image.asset(
          'assets/navbar.jpg', // Chemin de l'image ajoutée
          fit: BoxFit.contain,
          height: 70,
          width: 67,
        ),
        iconTheme: IconThemeData(
          color: Colors.white,
          size: 40, // Changer la couleur de l'icône du drawer en blanc
        ),
      ),
      endDrawer: CustomDrawer(), // Use the CustomDrawer here
      body: Center(
        child: Text("Home Page"),
      ),
    );
  }
}*/



